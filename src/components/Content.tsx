import { css } from '@emotion/react';

const Content = () => {
  return (
    <div
      css={css({
        backgroundColor: 'white',
        padding: '1rem',
        borderRadius: '0.5rem',
      })}
    >
      <h1>Content</h1>
    </div>
  );
};

export default Content;
